<Page
    x:Class="TUMCampusApp.Pages.NewsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TUMCampusApp.Pages"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <Page.Resources>
        <Style TargetType="controls:DropShadowPanel" x:Key="ShadowPanelStyle">
            <Setter Property="BlurRadius" Value="3.6"/>
            <Setter Property="ShadowOpacity" Value="0.52"/>
            <Setter Property="OffsetX" Value="5"/>
            <Setter Property="OffsetY" Value="5"/>
            <Setter Property="Margin" Value="15, 10, 15, 0"/>
            <Setter Property="Color" Value="Black"/>
        </Style>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPressedForegroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <ProgressBar x:Name="progressBar" IsIndeterminate="True" Visibility="Collapsed"/>
            <Grid Grid.Row="1" Background="{ThemeResource AppBarItemPointerOverBackgroundThemeBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Uid="DisableSources" Grid.Column="0" Text="Disable Sources" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
                <Button x:Name="expand_btn" Grid.Column="1" FontFamily="Segoe MDL2 Assets" Content="&#xE019;" Background="Transparent" FontSize="30" Click="expand_btn_Click" Margin="10,10,0,10" HorizontalAlignment="Right"/>
            </Grid>
            <ScrollViewer x:Name="newsSources_scv" HorizontalAlignment="Stretch" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Grid.Row="2" Visibility="Collapsed" Height="310" Background="{ThemeResource AppBarItemPointerOverBackgroundThemeBrush}">
                <StackPanel x:Name="newsSources_stckp" Padding="0,0,5,0" VerticalAlignment="Stretch">
                    <!--News Sources-->
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <controls:PullToRefreshListView x:Name="refresh_pTRV" RefreshRequested="refresh_pTRV_RefreshRequested" OverscrollLimit="0.4" PullThreshold="100" IsPullToRefreshWithMouseEnabled="True" Grid.Row="1" SelectionMode="None">
            <controls:PullToRefreshListView.PullToRefreshContent>
                <TextBlock Text="Pull down to refresh data" Margin="0,0,0,10" FontSize="20" Opacity="0.5"/>
            </controls:PullToRefreshListView.PullToRefreshContent>
            <StackPanel x:Name="news_stckp" Margin="0,0,0,10" VerticalAlignment="Stretch">
                <!--News-->
            </StackPanel>
        </controls:PullToRefreshListView>
    </Grid>
</Page>
