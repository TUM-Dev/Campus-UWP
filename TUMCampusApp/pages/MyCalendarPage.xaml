<Page
    x:Class="TUMCampusApp.Pages.MyCalendarPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TUMCampusApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ProgressBar x:Name="progressBar" IsIndeterminate="True" Visibility="Collapsed" Grid.Row="1"/>
        <ScrollViewer HorizontalAlignment="Stretch" Grid.Row="1">
            <StackPanel>
                <StackPanel x:Name="calendarEntries_stckp" Margin="10,0,10,0">
                    <TextBlock Text="Date:" Foreground="{ThemeResource ApplicationPressedForegroundThemeBrush}" Margin="10,20,0,0" HorizontalAlignment="Center"/>
                    <Rectangle Height="2" Fill="{ThemeResource ApplicationPressedForegroundThemeBrush}" Margin="0,5,0,5"/>
                    <!--Calendar Entries-->
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
