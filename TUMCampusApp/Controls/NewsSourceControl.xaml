<UserControl
    x:Class="TUMCampusApp.Controls.NewsSourceControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TUMCampusApp.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="50*"/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="title_tb" TextTrimming="CharacterEllipsis" Grid.Column="0" Text="Title" Margin="10"/>
        <CheckBox x:Name="enabled_chbx" x:Uid="enabled_chbx" Content="Enbaled" Grid.Column="1" HorizontalAlignment="Right" Margin="10" VerticalAlignment="Top" Checked="enabled_chbx_Checked_Changed" Unchecked="enabled_chbx_Checked_Changed"/>
    </Grid>
</UserControl>
