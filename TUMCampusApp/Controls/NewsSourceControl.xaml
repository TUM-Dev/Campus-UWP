<UserControl x:Class="TUMCampusApp.Controls.NewsSourceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Background="{StaticResource WidgetPrimaryAcrylicElementBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="title_tb"
                   Grid.Column="0"
                   Margin="10"
                   VerticalAlignment="Center"
                   Text="Title"
                   TextTrimming="CharacterEllipsis"/>
        <CheckBox x:Name="enabled_chbx"
                  x:Uid="enabled_chbx"
                  Grid.Column="1"
                  Margin="10"
                  HorizontalAlignment="Right"
                  VerticalAlignment="Center"
                  Checked="enabled_chbx_Checked_Changed"
                  Content="Enabled"
                  Unchecked="enabled_chbx_Checked_Changed"/>
    </Grid>
</UserControl>
