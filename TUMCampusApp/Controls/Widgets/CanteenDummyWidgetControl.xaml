<UserControl x:Class="TUMCampusApp.Controls.Widgets.CanteenDummyWidgetControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Loaded="UserControl_Loaded"
             mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    Background="{StaticResource WidgetSecondaryAcrylicElementBrush}">
            <TextBlock x:Name="header_tbx"
                       x:Uid="CanteenDummyWidgetControlHeader"
                       Margin="0,5,0,5"
                       HorizontalAlignment="Center"
                       Text="Loading canteens..."/>
        </StackPanel>
    </Grid>
</UserControl>
