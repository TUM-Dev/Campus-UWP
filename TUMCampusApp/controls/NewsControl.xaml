<UserControl
    x:Class="TUMCampusApp.Controls.NewsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Loaded="UserControl_Loaded"
    Tapped="UserControl_Tapped">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="10">
            <TextBlock x:Name="title_tbx" x:Uid="Loading" Text="Loading..." FontSize="20" FontWeight="ExtraBold" TextWrapping="WrapWholeWords" Margin="0, 0, 0, 10"/>
            <controls:ImageEx x:Name="image_img" IsCacheEnabled="True" HorizontalAlignment="Center" Margin="0, 10, 0, 10"/>
            <TextBlock x:Name="src_tbx" TextWrapping="WrapWholeWords"/>
            <TextBlock x:Name="date_tbx" TextWrapping="WrapWholeWords"/>
        </StackPanel>
        <ProgressRing x:Name="loading_ring" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" Width="100" Height="100"/>
    </Grid>
</UserControl>
