<UserControl
    x:Class="TUMCampusApp.Controls.CanteenControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="52*"/>
            <ColumnDefinition Width="17*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Padding="10,10,10,10" Grid.Column="0">
            <TextBlock x:Name="canteenName_tbx" FontSize="25" TextTrimming="CharacterEllipsis"/>
            <TextBlock x:Name="canteenAdress_tbx" FontSize="20" TextTrimming="CharacterEllipsis"/>
        </StackPanel>
        <TextBlock x:Name="canteenDistance_tbx" FontSize="20" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="#FF0065BD" Grid.Column="1"/>
    </Grid>
</UserControl>
